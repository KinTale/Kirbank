import { Button } from "react-bootstrap"

function Login() {
    return (
      <div className="App">
        <header className="">Why does the button not work below</header>
        <Button variant="light">Login</Button>
      </div>
    );
  }
  
  export default Login;